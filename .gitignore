# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Environment files — all variants with real credentials are gitignored.
# Only .env.example is committed (no real values).
# Run `vite --mode test` or `vite --mode production` to select environment.
.env
.env.test
.env.production
